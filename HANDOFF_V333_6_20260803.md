# MARKET BASE V333.6 引き継ぎ資料

## ビルド

- Build ID: `MARKET_BASE_V333_6_RADIO_DOCK_READING_HALF_PC_UNIFICATION_20260803`
- Cache token: `20260803-v333-6-radio-dock-reading-half-pc-unification`
- 更新元: V333.5

## 今回の修正

### ラジオ／旧ツールタブ

`market-base-scroll-controls-r11328.js` が旧V331更新制御を動的に読み込み、各ページに明示されたV333更新制御と競合していました。読み込み順によって旧ツールドックが一瞬表示されたり、ラジオ小タブの外観がページごとに変わったり、ホーム復帰時にラジオタブが一瞬消えたりする原因です。

V333.6ではスクロール制御もV333更新制御だけを参照します。旧V331ツールドックは生成されません。ラジオ再生本体は既存の別タブ方式を維持しています。

PCではレイアウト幅（スクロールバーを除く）を基準に右端位置を計算し、8pxの余白を残します。折り畳み時もRADIOタブ全体が画面内に表示されます。

iPhoneがライブ放送を「前局／次局」ではなく±10秒ボタンで表示する場合に備え、左右のMedia Session操作を前の局／次の局にも割り当てました。iOS側のアイコンは変更できません。

### 読み物の半幅PC表示

「ラクダの目」で正常だった431〜899pxの外枠（左右16px、最大760px）を次の読み物と世界ラジオにも適用しました。各ページのヘッダー帯と本文を同じ外枠に揃えています。

- 曇り空の紳士
- 古典の一手
- 気持ち日和
- スートラの余韻
- 世界のラジオ

共通ファイルは `assets/css/market-base-reading-half-pc-v3336.css` です。

## 上書き方法

差分ZIPの中身をV333.5の公開ルートへ、ディレクトリ構造を保持したまま展開して上書きしてください。ファイルを削除する必要はありません。`sw.js`、`version.txt`、HTML、更新制御、オフラインマニフェストを同時に上書きすることが重要です。

配置後は一度「更新」を押してください。Service WorkerはV333.6のキャッシュ名へ切り替わり、V333.5の古いHTML／JSが残らない構成です。

## 確認項目

1. ホーム・各DB・学ぶページで、右端のラジオ小タブが同じ外観になる。
2. ホームへ戻ってもラジオタブが消えず、旧「ツール」タブが一瞬表示されない。
3. PCで折り畳んだRADIOタブが右端のスクロールバーに隠れず、全幅表示される。
4. iPhoneロック画面の左右ボタンで前局／次局へ切り替わる（対応するSafari／iOS）。
5. PCブラウザを画面半分にしたとき、4つの読み物と世界ラジオが430pxの細いスマホ幅に固定されず、ヘッダー帯と本文が同じ幅になる。
6. 通貨換算と計算機がV333.6のキャッシュへ更新される。

## 検査

`python3 scripts/test_v333_6_radio_dock_reading_half_pc.py`

静的回帰検査、JavaScript構文検査、manifest JSON検査を実施済みです。Playwrightが環境にないため、実ブラウザ自動描画検査は未実施です。
