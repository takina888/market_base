# MARKET BASE V273 R32U8L 専門DB「上へ」ボタン復元

- 全8つの専門データベースで、スマホ・タブレット画面右下の丸い「上へ」ボタンを復元しました。
- ボタンは共通JavaScriptから生成し、ページごとの有無の差をなくしました。
- 押すとスムーズスクロールでページ最上部へ戻ります。
- 旧ボタンがHTMLに残っているページでは、重複表示を防ぐため旧要素を除去します。
- ボタンは56〜58pxのタップ領域、青色SVG矢印、白背景、青系枠線で統一しました。
- PCでは既存のPC専用固定ナビゲーション内に「上へ」があるため、丸ボタンは非表示にして二重表示を防ぎます。
- 全8ページのCSS・JavaScript更新識別子を `r32u8l` に更新しました。
- 共通JavaScript構文、8ページの参照、ボタン生成処理、スムーズスクロール指定を確認しました。
